in signup page we take data from form and store it in the DB . 
we should not store the password as it is . 
when DB leaks .
when we made banking websites an we registers users . 
ahckers can take password and make transcations 


Hashing functions in any programming language . takes passwords as input and  it changes its form  {unreadable string which called as hashed form} .

When user logged in 
the password which user has entered  is again goes to the hashing function , and generates the hashed string 

the input and output will always be the same for hash function .
